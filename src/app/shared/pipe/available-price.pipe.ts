/*import { Pipe, PipeTransform } from '@angular/core';

@Pipe({name: 'availablePricePipe'})
export class AvailablePricePipe implements PipeTransform {
  transform(price: number, heroMoney: number): number {
    return price;
  }
}*/